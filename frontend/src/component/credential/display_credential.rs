use yew::prelude::*;

#[function_component(DisplayCredential)]
pub fn display_credential() -> Html {
    html! {
        <div>
            {"I am here to display a credential."}
        </div>
    }
}
